import { createZovaComponentPage } from 'zova';
import { ControllerPageIndex } from '../../page/index/controller.jsx';

export const ZPageIndex = createZovaComponentPage(ControllerPageHome, undefined, undefined);
