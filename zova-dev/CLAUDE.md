# Zova Dev Project

## File Operations

- Use `mv` instead of `git mv` for files not tracked by git (e.g., `src/module/demo-student/`). Test modules are not added to the git repository.
- After renaming a file, always `Read` the new path before `Write`, because the Write tool requires a prior Read at the same path.
